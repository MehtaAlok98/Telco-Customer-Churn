{
  "version": 2,
  "builds": [
    {
      "src": "api/app.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "1024mb",
        "requirements": "api/requirements.txt"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/app.py" }
  ]
}
